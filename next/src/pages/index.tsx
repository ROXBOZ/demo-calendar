import Calendar from "@/Components/Calendar";

const Home: React.FC = () => {
  return (
    <main className="flex min-h-screen flex-col gap-4 p-4">
      <Calendar />
    </main>
  );
};

export default Home;
